{
  "name": "movies-explorer-frontend",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "@testing-library/jest-dom": "^5.16.5",
    "@testing-library/react": "^13.4.0",
    "@testing-library/user-event": "^13.5.0",
    "normalize.css": "^8.0.1",
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "react-router-dom": "^5.2.1",
    "react-scripts": "5.0.1",
    "web-vitals": "^2.1.4"
  },
  "scripts": {
    "start": "react-scripts start",
    "dev": "cross-env PORT=4000 react-scripts start",
    "build": "react-scripts build --no-eslintrc",
    "test": "react-scripts test",
    "eject": "react-scripts eject",
    "lint": "stylelint **/*.{css,scss} --fix",
    "fix": "prettier --write . && eslint . --fix && stylelint **/*.{css,scss} --fix"
  },
  "eslintConfig": {
    "extends": [
      "react-app",
      "react-app/jest"
    ]
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  },
  "devDependencies": {
    "cross-env": "^7.0.3",
    "prettier": "^2.6.2",
    "prettier-plugin-organize-imports": "^3.2.1",
    "sass": "^1.54.4",
    "stylelint": "^14.14.0",
    "stylelint-config-standard-scss": "^6.0.0"
  }
}
