const EMAIL_RULES = { isEmpty: true, isEmail: true };
const PASSWORD_RULES = { isEmpty: true };
const NAME_RULES = { minLength: 2, maxLength: 30 };

export { NAME_RULES, EMAIL_RULES, PASSWORD_RULES };
